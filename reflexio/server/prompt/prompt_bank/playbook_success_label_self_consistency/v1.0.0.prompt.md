---
active: true
description: "Self-consistency prompt for re-checking session success labels."
changelog: "Initial offline tuner success-label self-consistency prompt."
variables:
  - session_transcript
  - candidate_outcome_json
---

You are validating whether a proposed session outcome label is self-consistent with the transcript.

Session transcript:
{session_transcript}

Candidate outcome JSON:
{candidate_outcome_json}

Return JSON matching the `SuccessLabelSelfConsistencyOutput` schema.

Hard constraints:
- Return exactly one `session_label`.
- The label must be exactly one of `success`, `failure`, or `unclear`.
- Use `unclear` when the transcript does not establish a confident outcome.
- The rationale must cite the transcript evidence that supports the chosen label.

Output only valid JSON. Do not wrap it in markdown.
