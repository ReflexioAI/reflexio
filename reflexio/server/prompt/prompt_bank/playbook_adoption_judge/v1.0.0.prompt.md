---
active: true
description: "Mode-B adoption judge for reconstructing whether shown playbooks were followed."
changelog: "Initial offline tuner Mode-B adoption judge prompt."
variables:
  - session_transcript
  - shown_playbooks_json
---

You are reconstructing playbook adoption from a logged session transcript.

Session transcript:
{session_transcript}

Shown playbooks JSON:
{shown_playbooks_json}

For each shown playbook, decide whether the agent followed it, ignored it, or violated it.

Return JSON matching the `ModeBAdoptionJudgeOutput` schema.

Hard constraints:
- Each label must be exactly one of `followed`, `ignored`, or `violated`.
- Use only the transcript and shown playbooks provided here.
- The rationale for each label must quote or paraphrase the concrete transcript evidence that supports the label.

Output only valid JSON. Do not wrap it in markdown.
