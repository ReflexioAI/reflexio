---
active: true
description: "Reformulates a search query without changing its language and extracts temporal signals"
changelog: "v2.1.0: preserve the source language and script unless translation was explicitly requested"
variables:
  - query
  - conversation_context_block
  - today_iso
---
Reformulate the user's search query and extract its temporal intent. Today's date: {today_iso}

Rewrite (standalone_query):
- Clean, standalone natural language: resolve pronouns, ellipsis, and implicit references from conversation context; expand abbreviations; fix grammar; normalize terminology.
- Preserve the query's source language and writing system. Do not translate unless the user explicitly asks for translation.
- Preserve named entities, product names, identifiers, and code exactly unless resolving an unambiguous abbreviation.
- No FTS syntax, OR operators, or synonym stuffing.
- When conversation context is provided, extract the core problem from the full conversation — the latest query alone may be vague, but earlier turns contain the real issue.

Temporal signals — read the question's time semantics:
1. Explicit window ("this week", "in the last N days"): start_days_ago = 7 (or N), end_days_ago = 0.
2. As-of / before a boundary ("as of last month", "before May"): end_days_ago = days since that boundary, no start. The answer must predate the boundary.
3. Current/latest value ("current deploy target", "latest rule for X", "what do I use now"): recency_dominant = true.
4. Present-tense question about a mutable fact or policy: wants_current = true.
5. No time expression and the fact is not mutable: leave all temporal fields unset/false. Do not invent windows.

Examples:
"agent failed to refund" → standalone_query: "agent failed to process refund" (no temporal signals)
"用户现在使用什么数据库？" → standalone_query: "用户当前使用的数据库", wants_current: true
"今週追加したルールは？" → standalone_query: "今週追加したルール", start_days_ago: 7, end_days_ago: 0
{conversation_context_block}
Query: {query}
